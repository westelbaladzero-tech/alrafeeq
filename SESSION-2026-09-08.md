# جلسة 8 سبتمبر 2026 — الأمن والرؤية المستقبلية

> الحمد لله الذي هدانا لهذا وما كنا لنهتدي لولا أن هدانا الله
> قل هو الرحمن، آمنا به وعليه توكلنا

---

## ✅ ما أنجزناه اليوم (بفضل الله)

### 1. حماية رمز PIN — معزّز

```
✅ scrypt (ذاكرة-شاقة، مقاوم للقوة الغاشمة)
✅ Salt لكل مستخدم (الإيميل)
✅ قفل الحساب بعد 5 محاولات (15 دقيقة)
   ← في login + verify-pin معاً
✅ Rate limiting: 5 محاولات/دقيقة لكل IP
✅ عدّاد المحاولات المتبقية
✅ PIN لا يُخزّن نصاً صريحاً أبداً
✅ إعادة تصفير العدّاد عند النجاح
```

الملفات المعدّلة:
- src/app/api/verify-pin/route.ts — أضيف قفل الحساب + عدّاد
- src/app/api/auth/login/route.ts — كان به القفل مسبقاً

### 2. تشفير الرسائل من طرف لطرف (E2EE)

```
✅ مكتبة التشفير: src/lib/e2e-crypto.ts
✅ ECDH P-256 لمبادلة المفاتيح
✅ AES-GCM (256-bit) لتشفير الرسائل
✅ المفتاح الخاص في IndexedDB (محلي، لا يغادر الجهاز)
✅ المفتاح العام في profiles.pubkey (سحابي)
✅ تشفير الرسالة قبل الإرسال (ENC: + base64)
✅ فكّ التشفير عند الاستلام
✅ الرسائل القديمة غير المشفّرة تعمل عادي
✅ الخادم يرى نصاً عشوائياً فقط
```

الملفات:
- src/lib/e2e-crypto.ts — مكتبة التشفير الكاملة
- src/components/FriendsView.tsx — دمج التشفير في الشات

### 3. نسخة احتياطية سحابية للمفتاح الخاص

```
✅ المفتاح الخاص يُشفّر بالـ PIN قبل رفعه للسحاب
✅ PBKDF2 (100,000 iterations) + AES-GCM
✅ يُخزّن في profiles.encrypted_privkey
✅ عند فقدان الجهاز: المستخدم يدخل PIN ← يسترجع مفتاحه
✅ نافذة استرجاع المفتاح في FriendsView
✅ الخادم لا يرى المفتاح الخاص أبداً (مشفّر بالـ PIN)
```

الأعمدة المضافة:
- profiles.pubkey — المفتاح العام (نص)
- profiles.encrypted_privkey — المفتاح الخاص المشفّر (نص)

### 4. حماية APIs من الإساءة

```
✅ Rate limiting على كل APIs الحساسة
✅ تحقق المدخلات (XSS, PIN, مبالغ)
✅ RLS على كل الجداول
✅ bucket تخزين خاص (alrafeeq-files)
✅ قائمة بيضاء لأنواع الملفات
✅ حد حجم 50MB
```

---

## 🔮 الرؤية المستقبلية — نقاش الجلسة

> هذه رؤية مستقبلية تمت مناقشتها يوم 8 سبتمبر 2026
> تُنفّذ بإذن الله حين يحين وقتها

### تبويبات الرفيق الأمين — الصورة الكاملة

التطبيق الحالي:
  📱 شات الأصدقاء    ← تواصل آمن مشفّر
  💰 الحسابات        ← ديون + أقساط
  🤖 الرفيق الذكي    ← مساعد مالي

التوسعات المستقبلية:
  🏪 متجري           ← منصة موحدة
  📣 الإعلانات       ← تسويق + نشر
  📝 منشورات الأصدقاء ← سوشيال فيد
  📊 منشورات إعلانية  ← معرض منتجات
  💳 محفظة + P2P    ← تحويلات داخلية
  🩺 الصحة التجارية  ← لوحة تحكم شاملة
  📄 PDF + فواتير   ← مستندات رسمية

---

### 1. الرفيق الإعلاني (تبويب التسويق)

الوظيفة:
  ← يأخذ معلومات البرند من موقع التاجر
  ← يولّد محتوى تلقائياً بالـ AI
  ← يرفعه لمنصات التواصل
  ← يتتبع أداء الحملات

التنفيذ:
  ← الكود موجود مسبقاً عند المؤسس
  ← Gemini API جاهز للتكامل
  ← يحتاج: ربط APIs المنصات (Meta, TikTok, إلخ)

القيمة:
  ← محتوى يومي تلقائي
  ← حضور مستمر على المنصات
  ← توفير وقت التسويق اليدوي

---

### 2. المتجر الموحد (مثل إيزي أوردر)

الوظيفة:
  ← التاجر يفتح تبويب "متجري"
  ← يضيف: اسم، شعار، منتجات، أسعار
  ← يحصل على رابط: alrafeeq.app/shop/اسمه
  ← العميل يفتح الرابط → يرى المنتجات → يطلب
  ← الطلب يصل للشات مباشرة

التنفيذ:
  ← قالب واحد، بيانات مختلفة
  ← كل شيء في قاعدة البيانات
  ← تحديث واحد يطبّق على الجميع
  ← ربط مباشر بالرفيق

الجداول المطلوبة:
  stores: id, user_id, name, logo, phone, bio
  products: id, store_id, name, price, image, description
  orders: id, store_id, customer_phone, items, total, status

الصفحات:
  /shop/[id] ← واجهة المتجر العامة
  تبويب "متجري" ← لوحة تحكم التاجر

الربط:
  ← طلب جديد → رسالة نظام في الشات
  ← دفعة → تسوية في الحسابات
  ← ملخص → الصحة التجارية

---

### 3. منشورات الأصدقاء + الإعلانية

منشورات الأصدقاء:
  ← سوشيال فيد داخلي
  ← صديقك ينشر: نص/صورة/حالة
  ← يظهر في تبويب منفصل
  ← يشبه حالة واتساب لكن أعمق
  ← التنفيذ: جدول posts + صفحة فيد — سهل

منشورات إعلانية:
  ← منشورات تجارية فقط
  ← التاجر ينشر منتجاته وعروضه
  ← يظهر لكل المستخدمين
  ← يشبه Marketplace
  ← يربط مباشرة بالمتجري
  ← التنفيذ: جدول ads + صفحة معرض — سهل

---

### 4. المحفظة الداخلية + P2P

المفهوم:
  ← كل مستخدم عنده محفظة (رصيد داخلي)
  ← تحويل P2P بين الأصدقاء
  ← الرفيق = القاضي والمنظّم
  ← يشبه Binance P2P لكن باللمسة الرفيقية

المراحل:
  المرحلة 1: P2P + تحكيم
    ← لا يحتاج ترخيص مالي
    ← لا يحتاج شراكة بنكية
    ← التحويل يحدث خارجياً (فودافون كاش، إنستاباي)
    ← الرفيق ينظّم ويحكم بالأدلة
    ← يبدأ فوراً

  المرحلة 2: شحن/سحب يدوي
    ← حساب بنكي تجاري
    ← المستخدم يُودع + يرفع الإيصال
    ← الرفيق يائكد ويُزيد الرصيد
    ← سحب بطلب + تحويل يدوي

  المرحلة 3: بوابة دفع
    ← تكامل API بنكية
    ← شحن فوري + سحب فوري
    ← محفظة حقيقية كاملة

آلية التحكيم:
  1. المرسل يختار وسيلة الدفع
  2. المستلم يعرض بياناته + يوافق
  3. المعاملة تُقفل (المبلغ يُحجز)
  4. المرسل يحوّل خارجياً + يرفع إيصال
  5. المستلم يتحقق + يضغط "تم الاستلام"
  6. المبلغ المحجوز ينتقل للمستلم
  7. عند الخلاف: الأدلة + AI يحلل الإيصالات
  8. سمعة المستخدم تُبنى بمرور الوقت

الجداول:
  wallets: user_id, balance, updated_at
  wallet_transactions: id, from_user, to_user, amount, type, status, encrypted_details

الربط:
  ← سداد دين من المحفظة ← تسوية تلقائية
  ← دفع طلب متجر ← تأكيد تلقائي
  ← الصحة التجارية تحسب من المعاملات

---

### 5. بوابة الدفع الخارجية

الوظيفة:
  ← التعاقد مع بوابة دفع (Paymob, Fawry, Stripe)
  ← يحصل التطبيق على API + رابط دفع + Webhook
  ← المستخدم يشحن محفظته عبر البوابة
  ← يدفع بـ: بطاقات/محافظ/نقدي/عملات رقمية
  ← البوابة تُشعر الرفيق ← الرصيد يُضاف فوراً
  ← السحب: طلب ← تحويل عبر API ← يصله في محفظته

بوابات الدفع المتاحة:
  محلية: Paymob, Fawry, Opay, Khazna, Aman
  دولية: Stripe, PayPal, Skrill, Payoneer

الميزة:
  ← تنوع وسائل الدفع (500+ طريقة)
  ← لا نحتاج بناء بنية تحتية للدفع
  ← البوابة تتعامل مع الأموال
  ← الرفيق يتابع ويسجّل

---

### 6. الصحة التجارية

الوظيفة:
  ← لوحة تحكم شاملة لتجارة المستخدم
  ← تجمع كل البيانات في مكان واحد

الأقسام:
  💰 المال:
     ← لك كم؟ عليك كم؟
     ← تدفقات شهرية
     ← أقساط قادمة

  📊 التجارة:
     ← نمو العملاء
     ← متوسط الصفقة
     ← موسمية المبيعات

  📣 التسويق:
     ← أداء الحملات
     ← عائد الإعلان (ROI)
     ← نمو المتابعين

  🩺 التشخيص:
     ← نقاط قوتك
     ← مخاطر محتملة
     ← توصيات الرفيق الذكي

  النتيجة: رقم واحد "صحة تجارتك: 82/100"

---

### 7. PDF + فواتير + خدمات تجارية

PDF + فواتير:
  ← توليد فواتير رسمية PDF
  ← إرسال للعملاء
  ← تتبع حالة الدفع
  ← ربط بالحسابات

خدمات تجارية:
  ← عروض أسعار
  ← عقود مبسّطة
  ← إدارة عملاء (CRM مبسط)

---

## 📋 ترتيب التنفيذ المقترح

المرحلة الحالية (مكتملة):
  ✅ شات آمن مشفّر
  ✅ حسابات وديون
  ✅ رفيق ذكي
  ✅ أمان متكامل (PIN + E2EE + Rate Limiting)

المرحلة التالية:
  1. إصلاح أخطاء شات الأصدقاء
  2. تثبيت الأساس

بعدها:
  3. المتجر الموحد
  4. الرفيق الإعلاني
  5. منشورات الأصدقاء + الإعلانية
  6. المحفظة + P2P + تحكيم
  7. الصحة التجارية
  8. PDF + فواتير
  9. بوابة دفع خارجية (بعد التعاقد)

---

## 💡 ملاحظات من الجلسة

- المحفظة الداخلية + P2P لا يحتاج ترخيصاً مالياً للبدء
- بوابة الدفع الخارجية تحتاج تعاقداً مع مزود
- البطاقات الافتراضية (مثل باي بت) للأداء فقط، لا للاستقبال
- فودافون كاش يحوز مالاً حقيقياً — نحن نبدأ بتنظيم التحويلات الخارجية
- التشفير الكامل (E2EE) للشات فقط — الحسابات تستخدم RLS (تحتاج استعلامات SQL)
- شات الرفيق (AI) لا يُشفّر E2E لأن الخادم يحتاج قراءته لإرساله لـ Gemini

---

## 🎯 المبدأ الموجّه

"أقل ميزات، كلها تعمل، بسيطة
أفضل من
كثير ميزات، نصفها معطل، معقد"

نبدأ بما نملك
نمشي خطوة خطوة
لا نضيف ميزة حتى نثبّت ما قبلها
ولا نتنازل عن: الأمان، البساطة، العربية

---

> ربنا هب لنا من لدنك رحمة وهئ لنا من أمرنا رشدا
> وقل الحمد لله الذي هدانا لهذا وما كنا لنهتدي لولا أن هدانا الله
