# CHANGE SAFETY PROTOCOL

> **الغرض:** هذا الملف ليس توثيقًا لحالة النظام فقط، بل **منهج عمل إلزامي** قبل أي تعديل وأثناءه وبعده، حتى لا يتحول الإصلاح إلى إفساد.

> **المبدأ الحاكم:**
> **الإصلاح الأمني أو البرمجي المحلي قد يكون صحيحًا بذاته، لكنه قد يكسر الـ workflow العام إذا لم تُفهم الصورة الكاملة.**

---

## 1) تعريف هذا البروتوكول

هذا البروتوكول وُضع لتطبيق **الرفيق الأمين** بعد مراجعة شاملة شملت:
- كل مسارات `src/app/api` (31 route)
- طبقة قواعد البيانات والدوال (`RPC / SQL / triggers / indexes / constraints`)
- سياسات RLS
- منطق الـ workflows المالية والهوية والذكاء الاصطناعي والرفع
- أثر التعديلات المتفرقة على ترابط التطبيق

هذا البروتوكول يُستخدم عند:
- إصلاح bug
- إغلاق ثغرة أمنية
- تعديل status lifecycle
- إضافة field جديد
- تعديل route أو trigger أو RPC
- أي تغيير قد ينعكس على الـ DB أو الـ chat summaries أو الـ workflows

---

## 2) القاعدة الذهبية

قبل أي تعديل، اسأل:

> **هل أنا أصلح السطر، أم أفهم الدورة التي يعيش داخلها هذا السطر؟**

إذا أصلحت السطر فقط، فقد تفسد التطبيق.
إذا فهمت الدورة كاملة، يكون الإصلاح آمنًا.

---

## 3) قبل أي تعديل

### 3.1 حدّد الدومين بدقة
صنّف التعديل أولًا ضمن أحد الدومينات التالية:
- `auth`
- `admin`
- `p2p`
- `sanad / settlement`
- `relationship-change / friendships`
- `ai / chat / tts`
- `upload / file handling`
- `maintenance / cron`
- `db / rls / rpc / triggers`

### 3.2 اقرأ المرجع المعماري
قبل التعديل، ارجع إلى:
- `AUDIT.md` — لفهم الواقع الحالي، الترابط، والإصلاحات السابقة
- هذا الملف — لفهم **كيف** تعدّل بأمان

### 3.3 اسأل سؤالَي الحسم
قبل لمس أي كود:
1. **هل هذا التعديل يغيّر lifecycle أو status؟**
2. **هل هذا التعديل سيؤثر على Route آخر أو Trigger أو RPC أو Chat summary أو RLS؟**

إذا كانت الإجابة نعم على أي منهما، فالتعديل **ليس محليًا** ويجب التعامل معه كمراجعة معمارية، لا كسطر واحد فقط.

---

## 4) أثناء التعديل

### 4.1 لا تثق في حماية التطبيق وحدها إذا وُجدت حماية DB
إذا كانت الحماية ممكنة في قاعدة البيانات أيضًا، فالحماية يجب أن تكون على مستويين:
- **Application-level validation**
- **Database-level guarantee**

أمثلة:
- `UNIQUE INDEX / CONSTRAINT`
- `RLS`
- `REVOKE EXECUTE`
- `FK + CASCADE`

### 4.2 لا تقبل من العميل ما يجب اشتقاقه من السياق
لا تسمح للعميل بتمرير قيم حساسة يجب أن تُشتق من الجلسة أو العلاقات أو النظام، مثل:
- `status`
- `user_id`
- `from_user`
- `to_user` (إلا بعد التحقق أنه الطرف الآخر الصحيح)
- `confirmed_at`
- أي قيمة تؤثر مباشرة على ledger أو approval state

### 4.3 في التعديلات التي تمس الحالة (status)
إذا كان التغيير يكتب أو يحدّث `status`، فافحص دائمًا:
- من يكتب هذه الحالة؟
- من يقرأها لاحقًا؟
- هل هناك route آخر يفلتر فقط `confirmed` أو `pending`؟
- هل يوجد مسار انتقال واضح بين الحالات؟

**ممنوع** تغيير status دون فهم lifecycle كاملاً.

### 4.4 استخدم atomic updates في الحالات الحساسة
عند تحديث حالة مهمة أو عدّاد أو approval، يجب تقييد التحديث بالحالة الحالية أو القيمة الحالية.

أمثلة:
- `.eq("status", "pending")`
- `.eq("status", "verifying")`
- `.eq("failed_attempts", currentAttempts)`

هذا يمنع:
- race conditions
- lost updates
- overwriting final states

### 4.5 في مسارات المال، افترض وجود خصم يحاول كسر المنطق
في أي مسار مالي، اسأل دائمًا:
- هل يمكن لنفس الشخص تأكيد شيء لنفسه؟
- هل يمكن تجاوز approval step؟
- هل يمكن إرسال قيم status أو amount أو to_user مزورة؟
- هل يوجد race بين `confirm` و `cancel`؟
- هل side effects تُنفذ قبل القفل الذري؟

---

## 5) العلامات الحمراء (Red Flags)

إذا لمس التعديل أيًا من هذه الحقول أو المفاهيم، يجب أن تتوقف وتراجع الترابط كاملًا:
- `status`
- `user_id`
- `to_user`
- `from_user`
- `phone`
- `email`
- `friendship_id`
- `debt_request_id`
- `p2p_transaction_id`
- `sanad_message_id`
- `client_id`
- أي `secret`, `token`, `cron auth`
- أي file upload path أو external URL
- أي AI route يستهلك quota أو cost

---

## 6) أنماط الأخطاء التي يجب فحصها دائمًا

### 6.1 IDOR
مثال النمط الخاطئ:
- حذف أو تعديل بسطر مثل `.eq("id", id)` فقط

النمط الصحيح:
- `.eq("id", id).eq("user_id", userId)`

### 6.2 Mass Assignment
إذا كان العميل يمرر `status` أو `confirmed_at` أو ما شابه، فهذه علامة خطر.

النمط الصحيح:
- **ثبّت القيمة في الخادم**
- أو اشتقها من الـ workflow نفسه

### 6.3 Race Conditions
أي منطق يعتمد على:
1. قراءة الحالة
2. ثم تنفيذ side effects
3. ثم تحديث الحالة

فهو مرشح للانكسار تحت التزامن.

النمط الصحيح:
1. **اقفل الحالة أولًا atomic**
2. ثم نفّذ side effects

### 6.4 N+1 Queries
إذا كان route يجلب list ثم يعمل query إضافي لكل عنصر، فهذا إنذار أداء/DoS.

النمط الصحيح:
- Query واحد
- أو aggregate واحد
- أو RPC محسوب

### 6.5 Fail-open في المسارات الحساسة
إذا كان route حساسًا (`login`, `recover`, `verify-pin`, `admin`) ويستخدم rate limiting دون `strict=true`، فهذا خطر.

النمط الصحيح:
- **strict=true** للمسارات الحساسة
- fail-closed إذا تعطل الحامي

### 6.6 Timing Attacks
أي مقارنة على secrets أو admin sessions أو cron secrets يجب أن تستخدم مقارنة بزمن ثابت.

النمط الصحيح:
- `crypto.timingSafeEqual`
- ويفضل بعد hash إلى طول ثابت

---

## 7) مراجعة التكامل قبل اعتبار الإصلاح صحيحًا

بعد كل تعديل، اسأل:

### 7.1 هل أصلحت الثغرة الأصلية؟
### 7.2 هل كسرت وظيفة أخرى؟
### 7.3 هل التعديل محليًا صحيح، لكنه معماريًا خاطئ؟
### 7.4 هل يوجد Route آخر ما زال يتوقع السلوك القديم؟
### 7.5 هل الـ DB متوافقة مع ما غيّرته في الكود؟

إذا لم تُجب عن هذه الأسئلة، فالإصلاح غير مكتمل.

---

## 8) التحقق على 4 طبقات

أي تعديل حساس يجب أن يمر على هذه الطبقات الأربع:

### أ) طبقة الكود المحلي
- هل الكود نفسه صحيح؟
- هل فيه import مكرر؟
- هل فيه اسم متغير مكرر؟
- هل TypeScript/build سليم؟

### ب) طبقة workflow logic
- هل الـ flow ما زال يكتمل من البداية للنهاية؟
- هل هناك status transition مفقود؟
- هل الإصلاح خلق `pending forever`؟

### ج) طبقة DB / RLS / RPC / triggers
- هل هناك constraint أو index أو trigger يجب أن يتأثر؟
- هل RLS ما زالت تسمح بالسلوك الشرعي وتمنع غيره؟
- هل الدوال الحساسة عليها `REVOKE EXECUTE`؟

### د) طبقة أثر التغيير على بقية النظام
- هل `chat/route.ts` أو summaries أو analytics تقرأ هذا الجدول أو الحالة؟
- هل يوجد route آخر يعتمد على `confirmed` فقط؟
- هل هناك cron job أو cleanup logic يعتمد على الحالة؟

---

## 9) في التعديلات التي تمس قاعدة البيانات

قبل أي migration:
1. افحص الحالة الحالية في الـ DB
   - duplicates
   - pending rows
   - constraints الحالية
   - indexes الحالية
2. لا تضف constraint قبل التأكد أن البيانات الحالية تسمح به
3. بعد التنفيذ، تحقّق فعليًا من النتيجة باستعلام واضح

### قاعدة مهمة
**لا تفترض أن الـ DB غير محمية فقط لأن الكود يبدو كذلك.**
قد يكون هناك:
- unique index موجود
- constraint باسم غير متوقع
- trigger أو RPC يغطي جزءًا من المنطق

افحص قبل أن تصلح.

---

## 10) قواعد خاصة بالمسارات المالية

أي route مالي يجب أن يحقق الآتي:
- `to_user` = الطرف الآخر الصحيح (وليس أي UUID من العميل)
- `status` لا يأتي من العميل إذا كان حساسًا
- `amount` يمر عبر validation موحدة
- لا يسمح لنفس الطرف بتأكيد/حل/استرجاع ما لا ينبغي أن يحله بنفسه
- التحديثات النهائية يجب أن تكون atomic
- side effects لا تُنفذ قبل القفل الذري

---

## 11) قواعد خاصة بمسارات AI ورفع الملفات

أي route يستهلك AI أو يرفع ملفات يجب أن يحقق الآتي:
- rate limiting حاضر
- حد حجم الملف واضح
- التحقق من النوع واضح (`mime` + إن أمكن magic bytes)
- أي URL من العميل يجب التحقق منه
- أي path/folder من العميل يجب تقييده regex أو whitelist
- أي قيمة تؤثر على cost (`history`, `rate`, `text size`) يجب ضبطها أو clamp

---

## 12) بعد التعديل: التوثيق الإجباري

إذا كان التعديل حساسًا، فيجب تحديث:
- `AUDIT.md`

ويجب أن يذكر التوثيق:
1. المشكلة
2. سببها الجذري
3. الإصلاح
4. الأثر المعماري
5. هل تم التحقق من DB / workflow / RLS / routes الأخرى

الهدف ليس التوثيق للتجميل، بل منع تكرار سوء الفهم لاحقًا.

---

## 13) commit message rule

رسالة الـ commit يجب أن تشرح دائمًا:
- **ما المشكلة**
- **ما الإصلاح**
- **لماذا كان مهمًا**
- **ما الترابط الذي تم أخذه بالحسبان**

مثال جيد:
- race condition + atomic fix
- status contradiction + workflow correction
- strict rate limiting + fail-closed on sensitive route

---

## 14) متى نوقف التعديل ونراجع معماريًا؟

أوقف التنفيذ وادخل مراجعة معمارية إذا ظهر أحد هذه المؤشرات:
- الإصلاح يحتاج تغيير `status` لكن لا يوجد approval flow واضح
- هناك أكثر من route يقرأ الحالة بطرق مختلفة
- الإصلاح يتعارض مع `chat` أو summaries أو ledger logic
- هناك DB behavior غير واضح (constraint / trigger / RPC)
- الإصلاح يمنع التلاعب، لكنه يكسر التشغيل الطبيعي

هذا ليس فشلًا — بل هو التصرف الصحيح.

---

## 15) المخرجات المتوقعة من أي دورة تعديل صحيحة

أي دورة تعديل صحيحة يجب أن تمر بهذا التسلسل:
1. **نراجع**
2. **نكتشف**
3. **نصلح**
4. **نتحقق**
5. **نوثق**
6. **نربط الإصلاح بالصورة العامة**

إذا سقطت خطوة من هذه الخطوات، فالدورة ناقصة.

---

## 16) الوضع الخاص بمشروع الرفيق الأمين

في هذا المشروع ثبت عمليًا أن الأنواع التالية من الأخطاء كانت الأكثر خطورة:
- race conditions في المال والهوية
- mass assignment على `status`
- IDOR على الحذف أو القراءة
- fail-open في المسارات الحساسة
- timing attacks في admin/cron auth
- quota abuse في AI/file routes
- **التناقض بين الإصلاح الأمني المحلي وسلوك التطبيق العام**

لذلك، أي تعديل مستقبلي يجب أن يُفحص على هذه الأنواع أولًا.

---

## 17) الخلاصة التنفيذية

هذا البروتوكول يجب التعامل معه كالتالي:
- **مرجع إلزامي قبل أي تعديل حساس**
- **مرجع قرار أثناء المراجعة**
- **مرجع محاسبة بعد أي bug أو regression**

والقاعدة الأخيرة:

> **إذا لم تفهم الدورة كاملة، فلا تغيّر الحالة.**

> **وإذا لم تربط الإصلاح ببقية النظام، فلا تعتبره إصلاحًا مكتملًا.**
